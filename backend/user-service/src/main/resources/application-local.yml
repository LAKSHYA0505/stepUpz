spring:

  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/stepupz_user-Auth_db
    username: postgres
    password: postgres

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true